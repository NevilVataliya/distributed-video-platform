services:
  nginx-loadbalancer:
    image: nginx:latest
    container_name: hexanodes_lb
    ports:
      - "8080:8080" 
    volumes:
      - ./nginx-lb/nginx.conf:/etc/nginx/nginx.conf
    restart: unless-stopped